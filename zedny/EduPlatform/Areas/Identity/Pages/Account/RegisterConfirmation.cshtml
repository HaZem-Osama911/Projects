@page
@model RegisterConfirmationModel
@{
    ViewData["Title"] = "Confirm Registration";
}
<link rel="stylesheet" href="~/css/auth.css" />

<div class="auth-body">
    <div class="led-box">
        <div class="auth-card">
            <div class="brand-name-wrapper">
                <i class="bi bi-envelope-paper-heart-fill text-primary" style="font-size: 3rem; filter: drop-shadow(0 0 10px rgba(99, 102, 241, 0.4));"></i>
                <h1 class="brand-title mt-3" style="font-size: 1.8rem;">ZEDNY</h1>
                <div class="brand-subtitle">تأكيد التسجيل</div>
            </div>

            <div class="auth-subtitle-text px-2">
                @{
                    if (@Model.DisplayConfirmAccountLink)
                    {
                        <div class="alert alert-warning border-0 bg-dark text-warning small mb-4" style="border-radius: 12px;">
                            <i class="bi bi-exclamation-triangle me-2"></i>
                            نظام الإرسال التلقائي قيد التهيئة حالياً.
                        </div>
                        <p>لتفعيل حسابك الآن، يرجى الضغط على الرابط التالي:</p>
                        <a id="confirm-link" href="@Model.EmailConfirmationUrl" class="btn-auth d-inline-block text-decoration-none mt-2">تفعيل الحساب الآن</a>
                    }
                    else
                    {
                        <p class="fs-6">شكراً لانضمامك إلينا!</p>
                        <p>يرجى مراجعة بريدك الإلكتروني لتفعيل الحساب والبدء في رحلتك التعليمية.</p>
                        <div class="mt-4">
                            <i class="bi bi-hourglass-split animate-pulse" style="font-size: 1.5rem; color: #6366f1;"></i>
                        </div>
                    }
                }
            </div>

            <div class="auth-footer-links mt-4">
                <a asp-page="./Login" class="auth-link">العودة لتسجيل الدخول</a>
            </div>
        </div>
    </div>
</div>