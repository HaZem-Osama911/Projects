# منصة زدني التعليمية (EduPlatform)

## 📋 نظرة عامة على المشروع

**منصة زدني التعليمية** هي منصة تعليمية إلكترونية شاملة مبنية باستخدام **ASP.NET Core MVC** و **Entity Framework Core**. تتيح المنصة إدارة كاملة للعملية التعليمية من خلال ثلاثة أنواع من المستخدمين: **المدير العام (Super Admin)**، **المعلمين (Teachers)**، و **الطلاب (Students)**.

---

## 🎯 الهدف من المشروع

توفير نظام إدارة تعليمي متكامل يسمح بـ:
- إدارة المستخدمين والأدوار (Roles)
- ربط الطلاب بالمعلمين
- إنشاء وإدارة الكورسات التعليمية
- تسجيل الطلاب في الكورسات (Enrollments)
- عرض المعلمين والكورسات المتاحة

---

## 🛠️ التقنيات المستخدمة

### Backend Framework
- **ASP.NET Core 10.0** - إطار عمل الويب
- **Entity Framework Core 10.0.2** - ORM لإدارة قواعد البيانات
- **SQL Server** - قاعدة البيانات الرئيسية
- **ASP.NET Core Identity** - نظام المصادقة والتفويض

### Frontend Technologies
- **Razor Pages** - محرك عرض Razor
- **Bootstrap** - إطار عمل CSS
- **jQuery** - مكتبة JavaScript
- **CSS3** - تصميمات مخصصة

### Packages المستخدمة
```
- Microsoft.AspNetCore.Identity.EntityFrameworkCore (10.0.2)
- Microsoft.AspNetCore.Identity.UI (10.0.2)
- Microsoft.EntityFrameworkCore.SqlServer (10.0.2)
- Microsoft.EntityFrameworkCore.Tools (10.0.2)
- Microsoft.VisualStudio.Web.CodeGeneration.Design (10.0.2)
```

---

## 📁 هيكل المشروع

```
EduPlatform/
├── Areas/
│   └── Identity/
│       └── Pages/          # صفحات المصادقة (Login, Register, etc.)
├── Controllers/
│   ├── HomeController.cs   # الصفحة الرئيسية
│   └── MVCControllers/
│       ├── AdminPanel/      # لوحة تحكم المدير
│       │   ├── AdminController.cs
│       │   ├── RolesController.cs
│       │   ├── StudentsController.cs
│       │   └── TeachersController.cs
│       └── TeacherPanel/    # لوحة تحكم المعلم
│           ├── CoursesController.cs
│           └── TeacherPanelController.cs
├── Data/
│   ├── ApplicationDbContext.cs  # سياق قاعدة البيانات
│   └── Migrations/              # ملفات الهجرة
├── Models/
│   ├── ApplicationUser.cs       # نموذج المستخدم المخصص
│   ├── Course.cs                # نموذج الكورس
│   ├── Enrollment.cs            # نموذج التسجيل في الكورس
│   ├── StudentTeacher.cs        # نموذج علاقة الطالب-المعلم
│   └── ErrorViewModel.cs
├── ViewModels/
│   ├── CourseViewModel.cs
│   └── TeacherCardVM.cs
├── Views/
│   ├── Home/                    # الصفحة الرئيسية
│   ├── Courses/                 # صفحات الكورسات
│   ├── Roles/                   # صفحات إدارة الأدوار
│   ├── Students/                # صفحات الطلاب
│   ├── Teachers/                # صفحات المعلمين
│   └── TeacherPanel/            # لوحة تحكم المعلم
├── wwwroot/
│   ├── Asset/                   # الصور والموارد
│   ├── css/                     # ملفات CSS
│   ├── js/                      # ملفات JavaScript
│   └── uploads/profiles/        # صور المستخدمين المرفوعة
├── Program.cs                    # نقطة البداية
└── appsettings.json             # إعدادات التطبيق
```

---

## 🗄️ قاعدة البيانات

### الجداول الرئيسية

#### 1. **Users (Security Schema)**
جدول المستخدمين المخصص من `ApplicationUser`:
- `Id` (PK, string)
- `UserName` (string)
- `Email` (string)
- `FirstName` (string)
- `LastName` (string)
- `ProfileImage` (string) - اسم ملف الصورة الشخصية
- `CreatedAt` (DateTime)

#### 2. **Roles (Security Schema)**
جدول الأدوار:
- `SUPER_ADMIN` - المدير العام
- `TEACHER` - المعلم
- `STUDENT` - الطالب

#### 3. **Courses**
جدول الكورسات:
- `Id` (PK, int)
- `Name` (string, max 200)
- `Description` (string, max 2000)
- `Price` (decimal, 0-100000)
- `TeacherId` (FK → Users)
- `CreatedAt` (DateTime)

#### 4. **Enrollments**
جدول تسجيلات الطلاب في الكورسات:
- `Id` (PK, int)
- `CourseId` (FK → Courses)
- `StudentId` (FK → Users)
- `EnrolledAt` (DateTime)
- `IsActive` (bool)
- **Unique Constraint**: (CourseId, StudentId) - منع التسجيل المكرر

#### 5. **StudentTeachers**
جدول علاقة Many-to-Many بين الطلاب والمعلمين:
- `StudentId` (FK → Users)
- `TeacherId` (FK → Users)
- **Composite Primary Key**: (StudentId, TeacherId)

### العلاقات (Relationships)

1. **Course → Teacher**: Many-to-One
   - كل كورس له معلم واحد
   - عند حذف المعلم: Restrict (لا يمكن حذف المعلم إذا كان لديه كورسات)

2. **Enrollment → Course**: Many-to-One
   - كل تسجيل مرتبط بكورس واحد
   - عند حذف الكورس: Cascade (يتم حذف جميع التسجيلات)

3. **Enrollment → Student**: Many-to-One
   - كل تسجيل مرتبط بطالب واحد
   - عند حذف الطالب: Restrict

4. **StudentTeacher**: Many-to-Many
   - طالب واحد يمكن أن يكون مرتبط بعدة معلمين
   - معلم واحد يمكن أن يكون مرتبط بعدة طلاب

---

## 👥 أنواع المستخدمين والصلاحيات

### 1. **SUPER_ADMIN (المدير العام)**
- إدارة كاملة للنظام
- إدارة الأدوار (Roles)
- إدارة المعلمين (إضافة، تعديل، حذف)
- إدارة الطلاب (إضافة، تعديل، حذف)
- ربط الطلاب بالمعلمين
- عرض جميع الكورسات والطلاب

**حساب افتراضي:**
- Email: `hazemosama322@gmail.com`
- Password: `H@z111`

### 2. **TEACHER (المعلم)**
- إنشاء وإدارة الكورسات الخاصة به
- تعديل وحذف الكورسات الخاصة به
- عرض الطلاب المسجلين في كورساته
- عرض وإدارة الطلاب المرتبطين به

### 3. **STUDENT (الطالب)**
- عرض المعلمين المتاحين
- اختيار المعلمين المراد الالتحاق بهم
- عرض المعلمين المرتبطين به
- التسجيل في الكورسات (إن وجدت هذه الوظيفة)

---

## 🚀 الميزات الرئيسية

### 1. نظام المصادقة والتفويض
- تسجيل الدخول والخروج
- إدارة الأدوار تلقائياً عند بدء التطبيق
- حماية الصفحات بناءً على الأدوار
- رفع الصور الشخصية للمستخدمين

### 2. لوحة تحكم المدير (Admin Panel)
- **إدارة الأدوار**: إضافة، تعديل، حذف الأدوار
- **إدارة المعلمين**: 
  - عرض جميع المعلمين
  - إضافة معلم جديد
  - تعديل بيانات المعلم
  - حذف المعلم
  - ربط الطلاب بالمعلمين
  - عرض طلاب المعلم
- **إدارة الطلاب**:
  - عرض جميع الطلاب
  - إضافة طالب جديد
  - تعديل بيانات الطالب
  - حذف الطالب
  - اختيار المعلمين للطالب

### 3. لوحة تحكم المعلم (Teacher Panel)
- **إدارة الكورسات**:
  - عرض جميع الكورسات الخاصة بالمعلم
  - إضافة كورس جديد
  - تعديل كورس موجود
  - حذف كورس
  - عرض الطلاب المسجلين في كل كورس
- **إدارة الطلاب**:
  - عرض الطلاب المرتبطين بالمعلم
  - تعديل بيانات الطالب

### 4. الصفحة الرئيسية
- عرض جميع المعلمين المتاحين
- عرض عدد الكورسات لكل معلم
- تصميم حديث وجذاب مع تأثيرات بصرية

---

## ⚙️ الإعدادات والتكوين

### Connection String
```json
"ConnectionStrings": {
  "DefaultConnection": "Server=(localdb)\\mssqllocaldb;Database=EduPlatform;Trusted_Connection=True;MultipleActiveResultSets=true"
}
```

### إعدادات رفع الملفات
- الحد الأقصى لحجم الملف: **10 MB**
- مسار حفظ الصور: `wwwroot/uploads/profiles/`
- الصورة الافتراضية: `default.jpeg`

### إعدادات Kestrel
```json
"Kestrel": {
  "Limits": {
    "MaxRequestBodySize": 10485760  // 10 MB
  }
}
```

---

## 🔐 الأمان

1. **ASP.NET Core Identity**: نظام مصادقة قوي
2. **Authorization**: حماية الصفحات بناءً على الأدوار
3. **Anti-Forgery Tokens**: حماية من CSRF
4. **SQL Injection Protection**: استخدام Entity Framework Core
5. **Password Hashing**: تشفير كلمات المرور تلقائياً

---

## 📝 نماذج البيانات (Models)

### ApplicationUser
```csharp
- FirstName: string
- LastName: string
- ProfileImage: string (default: "default.jpeg")
- CreatedAt: DateTime
- FullName: string (computed property)
```

### Course
```csharp
- Id: int
- Name: string (required, max 200)
- Description: string (required, max 2000)
- Price: decimal (0-100000)
- TeacherId: string (FK)
- CreatedAt: DateTime
```

### Enrollment
```csharp
- Id: int
- CourseId: int (FK)
- StudentId: string (FK)
- EnrolledAt: DateTime
- IsActive: bool
```

### StudentTeacher
```csharp
- StudentId: string (FK)
- TeacherId: string (FK)
```

---

## 🎨 الواجهة الأمامية

### التصميم
- تصميم عصري مع تأثيرات Glass Morphism
- ألوان رئيسية: `#6366f1` (Indigo) و `#0ea5e9` (Sky Blue)
- تأثيرات انتقالية سلسة (Smooth Transitions)
- تصميم متجاوب (Responsive Design)

### الصفحات الرئيسية
1. **الصفحة الرئيسية (Home/Index)**: عرض بطاقات المعلمين
2. **صفحات المصادقة**: Login, Register, Logout
3. **لوحة تحكم المدير**: Dashboard للإدارة الكاملة
4. **لوحة تحكم المعلم**: Dashboard لإدارة الكورسات والطلاب

---

## 🔄 Migration و Database

### إنشاء Migration جديد
```bash
dotnet ef migrations add MigrationName
```

### تطبيق Migrations
```bash
dotnet ef database update
```

### Migrations الموجودة
- `CreateIdentitySchema` - إنشاء جداول Identity
- `AddApplicationUserFields` - إضافة حقول ApplicationUser
- `SeedRoles` - إنشاء الأدوار الافتراضية
- `UpdateProfileImage` - إضافة حقل الصورة الشخصية
- `AddStudentTeacherRelation` - إنشاء علاقة الطالب-المعلم
- `AddCoursesTable` - إنشاء جدول الكورسات
- `AddEnrollmentsTable` - إنشاء جدول التسجيلات

---

## 🚦 كيفية تشغيل المشروع

### المتطلبات الأساسية
1. **.NET 10.0 SDK**
2. **SQL Server** أو **SQL Server LocalDB**
3. **Visual Studio 2022** أو **Visual Studio Code**

### خطوات التشغيل

1. **استنساخ المشروع**
   ```bash
   git clone [repository-url]
   cd EduPlatform
   ```

2. **تحديث قاعدة البيانات**
   ```bash
   dotnet ef database update
   ```

3. **تشغيل المشروع**
   ```bash
   dotnet run
   ```
   أو من Visual Studio: اضغط `F5`

4. **الوصول للتطبيق**
   - افتح المتصفح على: `https://localhost:5001` أو `http://localhost:5000`

5. **تسجيل الدخول كمدير**
   - Email: `hazemosama322@gmail.com`
   - Password: `H@z111`

---

## 📊 المخططات والعلاقات

### مخطط قاعدة البيانات
```
Users (Security)
├── Courses (TeacherId → Users.Id)
│   └── Enrollments (CourseId → Courses.Id)
│       └── StudentId → Users.Id
└── StudentTeachers
    ├── StudentId → Users.Id
    └── TeacherId → Users.Id
```

---

## 🔧 التطوير المستقبلي

### ميزات مقترحة
- [ ] نظام تقييم المعلمين والكورسات
- [ ] نظام الدفع الإلكتروني
- [ ] نظام المحاضرات المباشرة (Live Sessions)
- [ ] نظام الواجبات والامتحانات
- [ ] نظام الإشعارات
- [ ] لوحة تحكم للطالب
- [ ] نظام الشهادات
- [ ] نظام الدردشة بين المعلم والطالب
- [ ] نظام التقارير والإحصائيات

---

## 📞 معلومات الاتصال

- **المطور**: Hazem Osama
- **Email**: hazemosama322@gmail.com

---

## 📄 الترخيص

هذا المشروع مخصص للاستخدام التعليمي.

---

## 🎓 ملاحظات مهمة

1. **الأدوار**: يتم إنشاء الأدوار تلقائياً عند أول تشغيل للتطبيق
2. **المدير العام**: يتم إنشاء حساب المدير العام تلقائياً إذا لم يكن موجوداً
3. **الصور**: الصور الشخصية تُحفظ في `wwwroot/uploads/profiles/`
4. **قاعدة البيانات**: يتم استخدام SQL Server LocalDB افتراضياً

---

## ✅ قائمة المهام المكتملة

- ✅ نظام المصادقة والتفويض الكامل
- ✅ إدارة الأدوار
- ✅ إدارة المستخدمين (معلمين وطلاب)
- ✅ ربط الطلاب بالمعلمين
- ✅ نظام الكورسات الكامل
- ✅ نظام التسجيل في الكورسات
- ✅ رفع الصور الشخصية
- ✅ واجهة مستخدم حديثة وجذابة

---

**تم إنشاء هذا الوصف بواسطة AI Assistant** 🤖

**آخر تحديث**: يناير 2025

